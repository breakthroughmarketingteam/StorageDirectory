<p>
	Your billing information has been updated and processed.
</p>
<table cellpadding="0" cellspacing="0">
	<tr>
		<td width="20"></td>
		<td width="340" valign="top" halign="left">
			<p>
				<% if @billable.is_a? Listing %>
					<strong>Facility Name:</strong><br>
					<a href="https://<%= $root_domain %>/my_account" target="_blank" style="color:green;"><%= link_to @billable.title, edit_listing_path(@billable) %></a>
				<% else %>
					<strong>Name:</strong><br>
					<a href="https://<%= $root_domain %>/my_account" target="_blank" style="color:green;"><%= link_to @billable.name, edit_client_path(@billable) %></a>
				<% end %>
			</p>
			<p>
				<strong>Amount:</strong><br>
				<%= @invoice.tran_amount %> for <%= @billable.is_a?(Listing) ? @billable.title : pluralize(@billable.listings.billable.size, 'listing') %>
			</p>
			<p>
				<strong>Recurring:</strong><br>
				Monthly on this date
			</p>
		</td>
	</tr>
</table>

<p>
	For more information about Billing: <a href="http://<%= $root_domain %>/help/call-tracking" target="_blank">http://<%= $root_domain %>/help/billing</a>
</p>