<%= render :partial => 'listings/rent_form', :locals => { :listing => listing, :size => (defined?(size) ? size : listing.get_searched_size(@search)), :special => predefined_special, :show_size_ops => show_size_ops } %>