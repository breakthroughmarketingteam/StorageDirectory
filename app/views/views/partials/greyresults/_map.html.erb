<div id="map_partial" class="inner inner_box">
	<h5>North Miami's Finest Climate Controlled Self Storage</h5>
	
	<div class="left">
		<div class="facility-intro">
			<p>All ground floor units with some upper level units accessible with a rolling ladder, and drive-up outdoor units also available.</p>
		</div>
		<%= display_listing_hours(listing) %>
	</div>
	
	<div class="google_map">
		<div id="map_<%= listing.map.id %>" rel="<%= "#{listing.lat},#{listing.lng}" %>" class="map_wrap"></div>
		<ul class="get_dir">
			<li>
				<% form_tag '/ajax/directions', :id => 'get_dirs' do %>
					<div>
						<input type="text" value="" title="For directions enter your starting address and... " name="from_address" id="from_address" class="directions hintable"> <%= link_to 'Go!', "/ajax/directions", :class => 'white submit_btn', :id => 'get_dirs' %> <%= submit_tag 'Go', :class => 'hide' %>
					</div>
				<% end %>
			</li>
		</ul>
	</div>
	
	<%= print_link '#map_partial' %>
</div>